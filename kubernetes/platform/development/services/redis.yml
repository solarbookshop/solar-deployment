apiVersion: apps/v1
kind: Deployment
metadata:
  name: solar-redis
  labels:
    db: solar-redis
spec:
  selector:
    matchLabels:
      db: solar-redis
  template:
    metadata:
      labels:
        db: solar-redis
    spec:
      containers:
        - name: solar-redis
          image: redis:8.4.0-alpine
          resources:
            requests:
              cpu: 100m
              memory: 50Mi
            limits:
              cpu: 200m
              memory: 100Mi

---

apiVersion: v1
kind: Service
metadata:
  name: solar-redis
  labels:
    db: solar-redis
spec:
  type: ClusterIP
  selector:
    db: solar-redis
  ports:
  - protocol: TCP
    port: 6379
    targetPort: 6379
